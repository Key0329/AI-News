# Implementation Plan: AI & AI Coding 自動化情報助手

**Branch**: `001-ai-news-assistant` | **Date**: 2026-01-06 | **Spec**: [spec.md](./spec.md)
**Input**: Feature specification from `/specs/001-ai-news-assistant/spec.md`

**Note**: This template is filled in by the `/speckit.plan` command. See `.specify/templates/commands/plan.md` for the execution workflow.

## Summary

建立一個自動化系統，定時從三層級資訊來源（AI 實驗室官方部落格、AI Coding 工具、開發框架與社群）蒐集 AI 與 AI Coding 相關資訊，使用 Google Gemini API 進行內容過濾、去重與摘要生成，並產生結構化的繁體中文摘要報告。系統採用 Node.js 實作，透過 GitHub Actions 定時排程執行，資料持久化使用本地 JSON 或 SQLite（遵循 MVP 原則），並支援配置檔案管理來源、環境變數管理敏感資訊。

## Technical Context

**Language/Version**: Node.js（NEEDS CLARIFICATION - 建議 LTS 版本如 v20.x 或 v22.x）
**Primary Dependencies**:
- RSS 解析器（NEEDS CLARIFICATION - 待研究如 `rss-parser`, `feedparser` 等）
- HTTP 客戶端（NEEDS CLARIFICATION - 待研究如 `axios`, `node-fetch` 等）
- Google Gemini API SDK（`@google/generative-ai`）
- 字串相似度計算（NEEDS CLARIFICATION - 待研究如 `string-similarity`, `natural` 等）
- 內容指紋演算法（NEEDS CLARIFICATION - 待研究如 `simhash`, crypto 內建 MD5）
- GitHub API 客戶端（NEEDS CLARIFICATION - 待研究是否使用 `@octokit/rest`）

**Storage**: 本地 JSON 檔案或 SQLite（NEEDS CLARIFICATION - 待根據資料結構複雜度決定，遵循 MVP 原則優先 JSON）
**Testing**: NEEDS CLARIFICATION（待研究 Node.js 測試框架如 Jest, Vitest, Mocha + Chai）
**Target Platform**: 本地機器或自有伺服器（Node.js 執行環境），使用 GitHub Actions Cron Job 定時排程
**Project Type**: single（命令列工具 + 定時排程）
**Performance Goals**:
- 5 分鐘內完成蒐集與摘要產生（SC-001）
- 單次 API 調用 30 秒超時（FR-024）
- AI API 摘要生成單篇處理時間 < 10 秒

**Constraints**:
- 零外部相依原則（constitution - 避免不必要框架）
- 效能優先（用戶需求）
- 並發蒐集上限 5 個來源（FR-025）
- 摘要報告最大 5MB（FR-027）
- 單一來源最大抓取 20 則（FR-021）

**Scale/Scope**:
- 三層級約 15-20 個資訊來源（RSS + GitHub API）
- 每日產生一份 Markdown 摘要報告
- 每次蒐集至少 5 則資訊（SC-002），最多 100 則（FR-041）
- 支援配置檔案管理（JSON/YAML）

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

### ✅ 高品質優先

- **錯誤處理**: spec 明確要求來源失敗不影響其他來源（FR-008）、AI API 失敗保留原始內容（FR-012）
- **日誌記錄**: 完整執行日誌包含時間戳、錯誤詳情、統計（FR-010）、敏感資訊遮蔽（FR-034）
- **資料驗證**: 必要欄位檢查（FR-031）、配置檔案驗證（FR-017）、環境變數驗證（FR-028）
- **結論**: ✅ PASS - 符合生產環境品質標準

### ✅ 可測試性（不可協商）

- **獨立測試**: 5 個 User Stories 均可獨立測試，每個都有明確的 Given-When-Then 場景
- **驗收標準**: 每個 User Story 包含 3-7 個可驗證的 Acceptance Scenarios
- **測試類型**: 支援功能測試（配置載入）、整合測試（API 調用）、契約測試（資料格式）
- **邊界案例**: 規格包含 Edge Cases 章節，涵蓋異常情況處理
- **結論**: ✅ PASS - 完全符合可測試性原則

### ✅ 最小可行產品（MVP）

- **優先級排序**: User Stories 明確標註 P1-P4，優先級理由清晰
- **核心功能優先**: P1（定時蒐集與摘要）是核心價值，P4（推送整合）屬增強功能
- **Out of Scope**: 明確列出 18 項不包含的功能（使用者管理、雲端部署、圖形化介面等）
- **MVP 範圍調整**: Session 2026-01-05 已移除 Twitter/GitHub 整合，聚焦 RSS + API
- **結論**: ✅ PASS - 符合 MVP 原則

### ⚠️ 避免過度設計（需審查）

**潛在風險**:
1. **功能需求數量**: 44 個功能需求（FR-001 ~ FR-044），是否過於詳細？
   - **分析**: 大部分是邊界條件處理（錯誤處理、限制、驗證），非額外功能
   - **判斷**: ✅ ACCEPTABLE - 這些是高品質軟體的必要要求，非過度設計

2. **三層級來源架構**: 是否需要在 MVP 階段支援三層級？
   - **分析**: spec 已在 Session 2026-01-05 簡化為 API 優先策略
   - **判斷**: ✅ ACCEPTABLE - 層級劃分是為了報告結構，非複雜抽象

3. **配置檔案系統**: 是否過早引入配置化？
   - **分析**: User Story 5（P2）明確要求配置化，理由是「易於維護和測試」
   - **判斷**: ✅ ACCEPTABLE - 配置化是基本要求，非過度設計

4. **模組化架構**: FR-020 要求模組化以支援擴展
   - **風險**: 可能導致過早抽象（YAGNI 違反）
   - **判斷**: ⚠️ NEEDS REVIEW - Phase 1 設計時需確認是否真的需要模組化架構，或簡單的函式分離即可

**結論**: ⚠️ CONDITIONAL PASS - 需在 Phase 1 設計時確認模組化架構的必要性

### ✅ 持續交付

- **小步快跑**: 可按 User Story 優先級逐步交付（P1 → P2 → P3 → P4）
- **獨立驗證**: 每個 User Story 完成後可獨立驗證
- **避免長期分支**: 單一功能分支 `001-ai-news-assistant`
- **結論**: ✅ PASS - 符合持續交付原則

### 📋 Phase 1 設計階段需重新檢查的項目

1. **模組化架構設計**: 確認是否需要複雜的模組註冊機制，或簡單的策略模式即可
2. **資料持久化選擇**: JSON vs SQLite - 根據實際資料結構複雜度決定，避免過早引入資料庫
3. **依賴項目選擇**: 確認是否需要 `@octokit/rest` 或直接使用 HTTP 客戶端調用 GitHub API

**整體評估**: ✅ PASS WITH REVIEW - 可進入 Phase 0 研究階段，但需在 Phase 1 設計時審查模組化複雜度

---

### 🔄 Phase 1 設計後重新評估（2026-01-06）

**評估時間**: Phase 1 Design 完成後
**評估文檔**: `data-model.md`, `quickstart.md`, `research.md`

#### ✅ 避免過度設計（重新評估結果）

**1. 模組化架構設計（已確認）**:
- **設計結果**: 採用簡單的函式組合，無複雜模組註冊機制
- **目錄結構**: `src/collectors/`, `src/filters/`, `src/summarizers/`, `src/generators/`
- **依賴注入**: 無，使用簡單的函式引入（`require`）
- **抽象層級**: 單層，無額外 `services/` 或 `repositories/`
- **評估**: ✅ PASS - 符合 YAGNI 原則，無過度抽象

**2. 資料持久化選擇（已確認）**:
- **設計結果**: JSON 檔案（`data/items.json`, `data/dedup-index.json`）
- **資料規模**: 每日 5-100 則，單檔案 <5MB
- **查詢需求**: 簡單過濾（`Array.filter()`），無複雜 JOIN
- **清理機制**: 每日自動清理，無長期儲存需求
- **升級路徑**: `data-model.md` 包含 SQLite 遷移方案（保留彈性）
- **評估**: ✅ PASS - JSON 完全符合 MVP 需求，避免過早引入資料庫複雜度

**3. 依賴項目選擇（已確認）**:
- **設計結果**: `@octokit/rest` v20.x（1.2MB）
- **選擇理由**:
  - GitHub Release 追蹤涉及 6-8 個來源（VS Code、Zed、Vercel AI 等）
  - 自動處理速率限制、分頁、重試邏輯
  - 減少手動實作 GitHub Rate Limit 追蹤的風險
- **風險分析**: 手動實作速率限制容易出錯，導致 API 被限流
- **成本權衡**: 1.2MB 依賴 vs 維護速率限制邏輯的工程成本
- **評估**: ✅ PASS - 符合「高品質優先」原則，依賴成本合理

**4. 資料模型複雜度（新增檢查）**:
- **實體數量**: 9 個核心實體（News Item, Source, Deduplication Index 等）
- **關聯關係**: 簡單一對多（Source → News Items），無多對多
- **驗證規則**: 使用簡單的 JSON Schema 或 JavaScript 驗證函式
- **無 ORM**: 不引入 Sequelize 或 TypeORM
- **評估**: ✅ PASS - 資料模型簡單清晰，無過度設計

**5. API 封裝層（新增檢查）**:
- **Gemini API**: 直接使用 `@google/generative-ai` SDK，無額外封裝
- **GitHub API**: 直接使用 `@octokit/rest`，無自定義 wrapper
- **RSS 解析**: 直接使用 `rss-parser`，無抽象層
- **評估**: ✅ PASS - 無過度封裝，直接使用原生 SDK

**6. 配置管理（新增檢查）**:
- **配置來源**: `config/sources.json` + `.env` 環境變數
- **無配置框架**: 不引入 `dotenv-flow`, `config`, `nconf` 等複雜框架
- **讀取方式**: 簡單的 `JSON.parse(fs.readFileSync())`
- **評估**: ✅ PASS - 配置管理簡單直接，符合 MVP 需求

#### 最終評估

| 原則 | Phase 0 評估 | Phase 1 評估 | 變更 |
|-----|------------|------------|-----|
| **高品質優先** | ✅ PASS | ✅ PASS | 無變更 |
| **可測試性** | ✅ PASS | ✅ PASS | 無變更 |
| **最小可行產品** | ✅ PASS | ✅ PASS | 無變更 |
| **避免過度設計** | ⚠️ CONDITIONAL PASS | ✅ PASS | ✅ 升級為 PASS |
| **持續交付** | ✅ PASS | ✅ PASS | 無變更 |

**Phase 1 設計階段結論**: ✅ **FULL PASS** - 所有設計決策符合憲法原則，無過度設計風險

**關鍵成就**:
1. 模組化架構保持簡單（函式組合 > 複雜框架）
2. 資料持久化採用 JSON（MVP 優先 > 過早優化）
3. 依賴選擇合理（風險控制 > 零依賴教條）
4. 資料模型簡單（實用主義 > 完美抽象）
5. 無過度封裝（直接使用 SDK > 自定義 wrapper）

**準備進入 Phase 2**: ✅ 可開始產生 `tasks.md`（透過 `/speckit.tasks` 命令）

## Project Structure

### Documentation (this feature)

```text
specs/[###-feature]/
├── plan.md              # This file (/speckit.plan command output)
├── research.md          # Phase 0 output (/speckit.plan command)
├── data-model.md        # Phase 1 output (/speckit.plan command)
├── quickstart.md        # Phase 1 output (/speckit.plan command)
├── contracts/           # Phase 1 output (/speckit.plan command)
└── tasks.md             # Phase 2 output (/speckit.tasks command - NOT created by /speckit.plan)
```

### Source Code (repository root)

```text
src/
├── collectors/          # 資料蒐集模組（RSS, GitHub API, NewsAPI 等）
├── filters/             # 內容過濾與去重邏輯
├── summarizers/         # AI 摘要生成（Gemini API 調用）
├── generators/          # 報告產生器（Markdown 格式化）
├── utils/               # 工具函式（相似度計算、日誌、配置載入）
├── models/              # 資料結構定義（TypeScript interfaces 或 JSDoc）
└── index.js             # 主程式進入點（CLI 執行）

config/
├── sources.json         # 資訊來源配置檔案（可選 YAML）
└── sources.example.json # 範例配置檔案

tests/
├── unit/                # 單元測試（各模組獨立測試）
├── integration/         # 整合測試（API 調用、檔案讀寫）
└── fixtures/            # 測試資料（模擬 RSS feed、API 回應）

output/
└── digests/             # 產生的摘要報告（YYYY-MM-DD-digest.md）

logs/                    # 執行日誌檔案

.github/
└── workflows/
    └── daily-digest.yml # GitHub Actions Cron Job 配置
```

**Structure Decision**:

採用 **Single Project** 結構，因為這是一個獨立的命令列工具。目錄設計遵循以下原則：

1. **模組化分離**: `collectors/`, `filters/`, `summarizers/`, `generators/` 分別處理蒐集、過濾、摘要、報告產生，符合單一職責原則
2. **配置分離**: `config/` 目錄存放資訊來源配置，支援版本控制（排除敏感資訊）
3. **測試結構**: 支援單元測試和整合測試，`fixtures/` 存放測試資料以避免真實 API 調用
4. **輸出分離**: `output/digests/` 和 `logs/` 分別存放報告和日誌，方便清理和查詢
5. **自動化支援**: `.github/workflows/` 存放 GitHub Actions 配置，實現定時排程

**重要設計決策**:
- 暫不引入複雜的模組註冊機制，採用簡單的函式組合
- Phase 1 設計時會決定是否需要進一步分層（如 `services/` 層）

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| [e.g., 4th project] | [current need] | [why 3 projects insufficient] |
| [e.g., Repository pattern] | [specific problem] | [why direct DB access insufficient] |
